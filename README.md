# Scuborga — bêta 0.1.1 split

Version issue du `index.html` monolithique 0.1.1 bêta, avec séparation minimale des fichiers :

- `index.html` : structure HTML
- `assets/styles.css` : styles extraits
- `src/app.js` : logique JavaScript extraite

Cette version conserve la disposition et le fonctionnement de la 0.1.1. Elle ne reprend pas la refonte UI 0.2.x.
