# Scuborga — bêta 0.3.0

Version basée sur le `index.html` monolithique 0.1.1 bêta, avec séparation minimale des fichiers :

- `index.html`
- `assets/styles.css`
- `src/app.js`

Cette version conserve la disposition et le fonctionnement validés de la 0.1.1. Elle ne reprend pas la refonte UI 0.2.x.

## Notes de version

- Passage à `0.3.0-beta`.
- Conservation de l’interface historique.
- Conservation du split minimal `src/` et `assets/`.
- Nouvelle clé locale : `scuborga_v0_3_0_beta`.
- Migration automatique depuis `scuborga_v0_1_1_beta`, `scuborga_v0_1_0_beta` et `treso_v16`.

## Déploiement GitHub Pages

Publier tous les fichiers à la racine du dépôt GitHub Pages.
